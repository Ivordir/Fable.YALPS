{
  "private": true,
  "scripts": {
    "postinstall": "dotnet tool restore",
    "start": "dotnet fable watch src -s -o fable",
    "build": "dotnet fable src -s -o fable"
  },
  "dependencies": {
    "yalps": "^0.5.4"
  }
}
